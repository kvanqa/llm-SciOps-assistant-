pyyaml>=6.0
numpy>=1.26
scikit-learn>=1.4
pypdf>=4.0
python-docx>=1.1
markdown-it-py>=3.0

# Optional (better semantic search — install if you want it and it's
# cleared under your data governance check; runs fully locally either way):
# sentence-transformers>=2.7
# faiss-cpu>=1.8

# Optional (Tier 1 local_llm mode):
# requests>=2.31   # already a transitive dep of most of the above, used to call Ollama

pytest>=8.0
